Stored Procedures PMS-Datenbank

Backlog anzeigen:

Delimiter //
create procedure backlog(in anzahl int)
Begin
 Select message from Backlog order by log_id limit anzahl;
End;
//

Benutzer einfügen:

Delimiter //
create procedure InsertUser(in Us_name varchar(20), pw varchar(100))
Begin
	Insert into user(name,passwort) values (Us_name,pw);
End;
//
